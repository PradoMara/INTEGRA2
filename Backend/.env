# --- Configuración de la Base de Datos (PostgreSQL) ---
# ¡CORREGIDO! Estos valores ahora coinciden con tu docker-compose.yml
DATABASE_URL="postgresql://appuser:appPass@localhost:5432/appdb"

# --- Configuración del Servidor ---
# ¡CORREGIDO! Puerto 3000 para que coincida con la config de tu frontend (api.ts)
PORT=3000
NODE_ENV=development

# --- Configuración de Autenticación (JWT) ---
JWT_SECRET=tu_jwt_secret_muy_seguro_aqui
JWT_EXPIRES_IN=7d

# --- Configuración de Seguridad (CORS) ---
# ¡CORREGIDO! Esta es la URL de tu frontend (Vite)
CORS_ORIGIN=http://localhost:5173

# --- Configuración de Encriptación (Bcrypt) ---
BCRYPT_ROUNDS=12